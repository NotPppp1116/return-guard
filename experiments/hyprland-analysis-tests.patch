diff --git a/tests/cases/safety.cpp b/tests/cases/safety.cpp
--- a/tests/cases/safety.cpp
+++ b/tests/cases/safety.cpp
@@ -1,4 +1,5 @@
 #include <cstdlib>
+#include <vector>
 
 int* cpp_return_stack_var() {
     int local = 42;
@@ -51,3 +52,25 @@ void cpp_safety_ignores_overloaded_shift() {
     ShiftSink sink;
     (void)(sink << 8);
 }
+
+struct RangeItem {
+    int value;
+};
+
+struct RangeOwner {
+    std::vector<RangeItem> items;
+
+    RangeItem* find_member_item() {
+        for (auto& item : items) {
+            if (item.value == 1)
+                return &item;
+        }
+        return nullptr;
+    }
+};
+
+RangeItem* find_parameter_item(std::vector<RangeItem>& items) {
+    for (auto& item : items) {
+        if (item.value == 1)
+            return &item;
+    }
+    return nullptr;
+}
